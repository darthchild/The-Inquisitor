<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.en.theinquisitor"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheInquisitor"
        tools:targetApi="31">
        <activity
            android:name="com.en.theinquisitor.GameModeActivity"
            android:exported="false" />
        <activity
            android:name="com.en.theinquisitor.QuestionActivity"
            android:exported="false"
            android:label="@string/title_activity_question"
            android:theme="@style/Theme.TheInquisitor" />
        <activity
            android:name="com.en.theinquisitor.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>